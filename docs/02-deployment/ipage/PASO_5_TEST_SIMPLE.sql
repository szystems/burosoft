-- ===================================================================
-- PASO 5: PROBAR CONSULTA SIMPLE
-- ===================================================================

SELECT 'PROBANDO SELECT SIMPLE:' as 'Info';
SELECT 'TEST_OK' as 'resultado' FROM resolucions_pa LIMIT 1;
